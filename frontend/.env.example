# CareerShield — Frontend Environment Configuration

# Production Backend API URL (Optional, defaults to relative /api)
# Set this on Vercel if your backend is hosted on Render / Railway / Serverless
VITE_API_URL=https://your-backend-api.onrender.com/api
